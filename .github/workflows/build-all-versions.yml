name: Build all versions

on:
  push:
    branches:
      - main
      - master
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  check-code:
    name: Check code
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 25
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: |
            17
            25

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Make Gradle wrapper executable
        run: chmod +x ./gradlew

      - name: Sync target properties
        run: |
          cp gradle/targets/26.2.properties versions/26.2-fabric/gradle.properties
          cp gradle/targets/26.2.properties versions/26.2-neoforge/gradle.properties

      - name: Inspect Gradle projects
        run: ./gradlew --no-daemon projects --stacktrace

      - name: Test preprocessor, all generated targets, config and math
        run: ./gradlew --no-daemon -p buildSrc test --stacktrace

      - name: Check active code
        run: ./gradlew --no-daemon check --stacktrace

  build:
    name: Build ${{ matrix.target }}
    runs-on: ubuntu-latest
    needs: check-code

    strategy:
      fail-fast: false
      matrix:
        target:
          - 1.20.1-forge
          - 1.21.1-fabric
          - 1.21.1-neoforge
          - 1.21.11-fabric
          - 1.21.11-neoforge
          - 26.1.2-fabric
          - 26.1.2-neoforge
          - 26.2-fabric
          - 26.2-neoforge

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 25
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: |
            17
            25

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Make Gradle wrapper executable
        run: chmod +x ./gradlew

      - name: Sync target properties
        run: |
          VERSION="${TARGET%-*}"
          cp "gradle/targets/${VERSION}.properties" "versions/${TARGET}/gradle.properties"
        env:
          TARGET: ${{ matrix.target }}

      - name: Switch Stonecutter target
        run: ./gradlew --no-daemon stonecutterSwitchTo${{ matrix.target }} --stacktrace

      - name: Build target
        run: ./gradlew --no-daemon :${{ matrix.target }}:build --stacktrace

      - name: Upload target jars
        uses: actions/upload-artifact@v4
        with:
          name: locatorbar-${{ matrix.target }}
          path: |
            versions/${{ matrix.target }}/build/libs/*.jar
            build/libs/**/*.jar
            !versions/${{ matrix.target }}/build/libs/*-sources.jar
            !versions/${{ matrix.target }}/build/libs/*-dev.jar
            !versions/${{ matrix.target }}/build/libs/*-javadoc.jar
            !build/libs/**/*-sources.jar
            !build/libs/**/*-dev.jar
            !build/libs/**/*-javadoc.jar
          if-no-files-found: warn
